package com.sudhindra.entity;

public enum Headers {
    ID("Id"),
    FIRST_NAME("firstName"),
    LAST_NAME("lastName"),
    SALARY("salary"),
    MANAGER_ID("managerId");

    Headers(String id) {
    }
}
